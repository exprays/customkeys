{
  "name": "nano",
  "private": true,
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "frontend",
    "cli"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.8.20"
  }
}
